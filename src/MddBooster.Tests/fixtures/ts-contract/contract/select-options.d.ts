// The `formSelectOptionsImport` contract — README "formSelectOptionsImport 가 가리킬 모듈".
//
// The generator requires exactly one argument and that the result drops straight into
// USelect's `options`. The return type is the consumer's to choose, so it is `unknown`
// here rather than a shape this repository does not get to decide.

export declare function enumToOptions(labels: Record<string, string>): unknown
