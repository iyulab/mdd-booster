{
  "name": "mdd-booster-ts-contract-gate",
  "private": true,
  "description": "Type-checks this generator's own TypeScript output against the consumer contract its README publishes.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/react": "19.2.2",
    "typescript": "5.7.3"
  }
}
