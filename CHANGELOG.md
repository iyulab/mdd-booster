# Changelog

이 파일은 **0.8.0부터** 기록한다. 그 이전 릴리스의 내용은 git 태그(`v0.3.0` ~ `v0.7.0`)와
커밋 이력을 참조할 것 — 사후에 재구성해 적으면 실제와 어긋날 수 있어 소급하지 않았다.

버전 정책: `0.MINOR.PATCH`. MINOR는 기능 추가, PATCH는 수정.

**여러 버전을 건너뛰어 올린다면 그 사이의 모든 항목을 읽을 것.** 각 릴리스는 자기 breaking만
적으므로, 최신 항목 하나만 읽으면 건너뛴 구간의 동작 변화가 그대로 남는다.

---

## [Unreleased]

### CLI 최상위 예외 출력 정리

`mdd build`가 실패할 때 원시 .NET 스택트레이스(~10줄)가 그대로 출력되던 문제를 고쳤다 —
이제 예외 메시지 한 줄만 출력한다. JSON 스키마 오류는 이미 메시지 자체에 `Path`/`LineNumber`/
`BytePositionInLine`이 포함돼 있어 정보 손실이 없다. 스택트레이스가 필요하면 `MDD_DEBUG`
환경변수를 설정한다(`MDD_DEBUG=1 mdd build`).

## 0.18.0

### `mdd.json` JSON Schema

에디터가 JSON Schema를 이해하면(예: VS Code) `mdd.json`을 저장 즉시 검증하고 타깃별 옵션을
자동완성할 수 있다. 모든 계층에서 `additionalProperties: false`를 강제해, 타깃 레벨 옵션을
설정 루트에 잘못 둔 경우(System.Text.Json이 알 수 없는 속성을 기본적으로 무시하므로 조용히
무시된다) 가장 흔한 실수 유형을 즉시 드러낸다. `$schema` 필드에
`https://raw.githubusercontent.com/iyulab/mdd-booster/main/schemas/mdd.schema.json`을 추가하면
바로 쓸 수 있다.

README의 `emitForeignKeyIndexes` 기본값 표기 오류도 함께 정정했다 — `0.13.0`에서
`false`→`true`로 바뀐 뒤 세 릴리스 동안 문서가 낡은 값을 그대로 두고 있었다.

### 실행 가능한 퀵스타트 샘플

README의 Model 타깃 퀵스타트가 실행 가능한 예제를 가리키지 않고 있었다. `samples/`에
`@reference`/`@lookup`/`@rollup`/`@computed`/enum을 쓰는 Project/Task 모델과 동작하는
`mdd.json`(Sql/Model/Api 타깃)을 추가했다 — `mdd build ./samples`로 바로 검증할 수 있다.
생성 결과물은 커밋 대상이 아니다(재생성되는 산출물).

### 파괴적 변경 없음

두 추가 모두 기존 모델·설정의 생성 결과에 영향을 주지 않는다.

## 0.17.1

### 순환 FullView 의존성 에러 메시지 — 필드 단위 귀속

`0.17.0`의 build-time 순환 감지는 순환 경로를 모델 이름만으로 나열했다(`Order -> OrderItem
-> Order`). Lookup이 많은 모델에서는 어느 필드가 각 홉을 만들었는지 역추적해야 했다. 이제
각 홉에 그 홉을 만든 필드와 종류가 붙는다: `Order -> OrderItem (via Order.SupplyTotal rollup)
-> Order (via OrderItem.CustomerName lookup)`. 순환 감지 로직 자체는 바뀌지 않았다 — 같은
그래프에서 귀속 정보만 함께 기록한다.

## 0.17.0

### 체이닝 Lookup + rollup 역방향 순환 뷰 의존성 — build-time 조기 실패

한 모델이 다른 모델의 파생(rollup) 필드를 lookup으로 체이닝하고, 그 다른 모델이 다시 앞
모델의 파생 필드를 되짚는 경우, `FullViewRenderer`가 모델 단위로는 각각 옳게 판단한 리다이렉트
두 개가 합쳐져 모델 간 순환을 만들 수 있었다. 지금까지는 `mdd build`가 이를 통과시켰고,
Schemorph 같은 선언형 스키마 도구가 실제로 뷰를 배포하려 할 때에야 SQL72009로 처음 드러났다.

이제 `mdd build`가 뷰를 쓰기 전에 순환을 그래프로 탐지해 순환 경로를 이름으로 나열하며
즉시 실패한다 — 배포 시점이 아니라 빌드 시점에 잡힌다. 자동 우회(순환 지점을 베이스
테이블+원 lookup 대상으로 재라우팅)는 이번 릴리스에 포함되지 않는다 — `IsDerivedColumn`
판정이 이미 한 번 회귀를 낸 민감한 지점이라 별도 트랙으로 신중히 다룬다.

### 생성 폼 `FormSection` — `sectionProps` 패스스루

소비 컴포넌트(`@iyulab/enterprise`의 `FormSection`)가 이미 받는 `className`/`style`을
생성기가 호출부에서 넘길 방법이 없었다. 이제 각 엔티티마다 `{Entity}FormSectionProps`
타입을 방출하고, 생성 폼(`{Entity}FormBase`)이 `sectionProps?: {Entity}FormSectionProps`를
받아 섹션 제목별로 `className`/`style`을 해당 `<FormSection>`에 그대로 전달한다. 소비앱은
이걸로 CSS 기반의 접기/숨김을 스스로 구현할 수 있다. 내장 접힘 시맨틱(`advancedSections`,
`@group`의 collapsible 옵션)은 이번 릴리스에 포함되지 않는다 — 실사용 사례가 하나뿐이라
UX(펼침 상태 기억 위치, 접힌 섹션의 검증 타이밍)를 지금 굳히기엔 이르다고 판단했다.

> **`className`/`style`은 새로 추가된 요구조건이다** — `sectionProps`를 넘기는지와 무관하게
> 모든 생성 폼의 `<FormSection>` 호출에 항상 방출되므로, 소비 중인 `FormSection` 컴포넌트가
> 이 두 프롭을 받지 못하면 업그레이드 즉시 컴파일이 깨진다. `disabled`(0.16.0)와 같은 종류의
> 요구조건 추가다.

---

## 0.16.0

### `@immutable` — 생성 폼에서 `disabled`, 생성 엔티티에서 `[Editable(false)]`

`@immutable`은 표준 필드 속성 카탈로그에 이미 등록돼 있었으나 지금까지 어느 타깃도 소비하지
않고 있었다.

**TypeScript 타깃**: 생성 폼의 네 컨트롤(`UInput`·`UTextarea`·`USelect`·`UCheckbox`) 전부에
`disabled` prop을 방출한다 — 필드는 계속 보이고 현재 값에 바인딩된 채로, 편집만 막힌다.
`readOnly`가 아니라 `disabled`를 선택한 이유: 체크박스·셀렉트는 네이티브 `readOnly` 의미가
컴포넌트마다 일관되지 않지만 `disabled`는 넷 다 동일하게 지원한다.

> **`disabled`는 새로 추가된 요구조건이다** — 소비 중인 컴포넌트 래퍼가 이 prop을 받지
> 못하면 갱신해야 한다.

**Model 타깃**: 생성 엔티티(쓰기·읽기 클래스 둘 다)에 `[Editable(false)]`를 방출한다 — TS
쪽과 같은 메타데이터일 뿐, 이 리포가 생성하는 어떤 API 계층도 이를 강제하지 않는다(두 타깃
모두 동일).

### `@unique`/`@index` → 생성 엔티티의 `HasIndex()`

SQL 타깃이 이미 UNIQUE 제약·인덱스로 방출하던 필드 단위 `@unique`/`@index`가 이제 Model
타깃에도 도달한다. `OnModelCreating`에 `modelBuilder.Entity<T>().HasIndex(...)`(`@index`) /
`.HasIndex(...).IsUnique()`(`@unique`)를 방출하며, 인덱스·제약 이름은 SQL Server(`UK_`/`IX_`)·
PostgreSQL(`uq_`/`ix_`) 방언이 실제로 쓰는 이름과 정확히 일치한다. 한 필드가 둘 다 선언하면
`unique`만 방출된다(제약이 이미 인덱스를 소유하므로 SQL 타깃과 동일한 배제). PK 필드는
제외된다(이미 유일 인덱스를 갖는다).

**섹션 레벨 복합 선언**(`@unique(c1, c2)`, `@index(c1)`)은 이번 릴리스에 포함되지 않는다 —
필드 단위 선언만 방출한다.

---

## 0.15.0

### 필드 단위 `@internal` — 저장하되 노출하지 않는다

`@internal`은 지금까지 모델(엔티티) 단위로만 쓸 수 있었다. 이제 필드에도 붙는다: 표시된
필드는 기반 테이블의 실컬럼, 쓰기 엔티티의 실프로퍼티로는 그대로 남지만, 읽기 표면에서는
빠진다 — `Sql` 타깃은 `FullView`/`UdView`의 `SELECT` 목록에서, `Model` 타깃은 인터페이스와
`*Ext`(읽기) 클래스에서 제외된다(`*Ext` 는 그 인터페이스를 구현하므로 인터페이스에서도 함께
빼야 컴파일된다). 파생 필드(Lookup/Rollup/Computed)에도 같은 술어가 적용되며, 그 결과 참조하는
FK가 오직 내부 전용 lookup에만 쓰였다면 해당 JOIN 자체가 뷰에서 사라진다.

두 타깃 모두 한 번의 빌드 안에서 같은 `mdd.json`이 만든 같은 `ResolvedModel`의 같은 필드
데이터에 같은 술어를 적용하므로, `Sql`과 `Model`의 짝이 서로 다른 시점에 어긋나는 경우는
생기지 않는다.

**`TypeScript` 타깃(생성 폼)은 이번 릴리스에 포함되지 않는다.** 표시된 필드는 계속 생성 폼에
나타난다 — 그 타깃까지 확장할지는 아직 미결이다.

`@internal`이 붙지 않은 필드의 생성 결과는 바이트 단위로 동일하다.

> **이미 배포된 `FullView`/`UdView`를 재생성하면 뷰 정의 자체가 바뀐다** — 필드를 새로
> `@internal`로 표시하면 그 컬럼이 `SELECT` 목록에서 빠지므로, 선언형 스키마 도구는 이를
> 뷰 재정의로 감지한다. 이는 이 기능이 만드는 새로운 위험이 아니라 뷰 `SELECT` 목록을 바꾸는
> 모든 모델 변경이 이미 갖는 배포 특성이다 — 재생성 전에 `diff`로 실제 변경 범위를 먼저 볼 것.

### `@label`이 필드 라벨을 명시적으로 오버라이드한다

`@label`은 파싱만 되고 어느 타깃도 읽지 않았다 — 라벨로 쓰이는 텍스트는 필드 설명(description)
뿐이었고, 설명이 없을 때 세 타깃이 서로 다르게 행동했다(`Model`은 `[Display]` 자체를 생략,
생성 TS 필드 스키마는 `label: null`, 생성 폼은 PascalCase 프로퍼티 이름으로 대체). 이제
`@label(text) → description → PascalCase 필드 이름` 우선순위가 공유 정본이고, 생성 폼은 이
정본을 직접 읽는다(입력 필드는 항상 어떤 라벨이든 표시해야 하므로).

`Model`의 `[Display(Name)]`과 TS 필드 스키마의 `label`은 계속 **명시된 것이 있을 때만**
채워진다(`@label(text) ?? description`, PascalCase 대체 없음) — 둘 다 원래 "적을 게 없으면
속성/필드 자체를 생략한다"는 기존 계약을 그대로 유지한다. 기계적으로 되풀이한 프로퍼티
이름은 그 두 자리에서는 정보가 아니라 잡음이기 때문이다.

`@label`을 쓰지 않는 필드의 생성 결과는 바이트 단위로 동일하다.

### `@help`가 `[Display(Description)]`로 방출된다

필드의 `@help` 텍스트가 이제 생성 엔티티의 `[Display(Description = "...")]`로 나온다 — 기존
`@label → Name`, `@group → GroupName` 매핑과 같은 자리, 같은 규칙이다: `[Display]`는
Name/GroupName/Description 중 하나라도 있을 때만 방출되고, 셋 다 없으면 생략된다.

### 파괴적 변경 없음

세 기능 모두 새 M3L 속성을 쓸 때만 생성 결과가 바뀐다. 기존 모델을 재생성해도 산출물은
동일하다.

---

## 0.14.0

### 🟡 절대경로 `projectPath`/`outputPath`가 다른 git 작업트리를 가리킬 때 경고

`mdd.json` 타깃이 절대경로(또는 `..`를 많이 쓴 상대경로)로 프로젝트 루트를 지정하면, 그 값은
`mdd build`를 어느 디렉터리에서 실행하든 항상 같은 곳을 가리킨다. 이 설정을 여러 물리적
체크아웃(예: linked git worktree와 그 메인 체크아웃, 또는 별도 클론 두 개)에서 그대로
재사용하면, 빌드는 지금 실행 중인 체크아웃이 아니라 그 경로가 실제로 위치한 체크아웃에
조용히 쓴다.

이제 `mdd build`가 호출된 디렉터리와 해석된 경로가 서로 다른 git 작업트리에 속하면 `stderr`로
경고한다. `Sql`·`Model`·`Api`·`TypeScript` 타깃 전부 적용된다(`projectPath`/`outputPath`/
`formsOutputPath`/`sqlProjectPath`). 경고이며 빌드를 막지 않는다 — 여러 체크아웃이 의도적으로
한 위치를 공유하는 구성일 수도 있다.

---

## 0.13.2

### 🔴 TS enum 생성기가 wire 이름이 아니라 CLR 스타일 이름을 방출했다 (버그)

`types/enums_gen.ts`의 union 리터럴과 `enum_labels_gen.ts`의 라벨/선택-맵 키가 M3L 멤버 이름을
PascalCase로 변환해 방출하고 있었다 — C# 생성기(`Enum_gen/*.cs`)가 이미
`[EnumMember(Value = "...")]`로 선언하는 wire 이름(M3L 원문 그대로, 예: `in_production`)과 다른 값이었다. 서버 쪽 EDM/JSON 계층이 `[EnumMember]`를 무시하는 동안에는
두 산출물이 우연히 같은 모양(CLR 이름)으로 맞아떨어지지만, 그 계층이 `[EnumMember]`를 따르는
순간 TS 쪽만 어긋난 값을 계속 주장하게 된다 — 같은 enum 선언의 두 생성물이 서로 다른 wire
계약을 주장해서는 안 된다는 것이 이 수정의 근거다.

TS 생성기가 C#과 같은 wire-이름 해석(멤버 선언 그대로)을 쓰도록 고쳤다 — 두 산출물이 같은
enum 선언에서 항상 같은 wire 계약을 가리킨다.

같은 원인이 폼 생성기에도 있었다: `@default(...)`가 붙은 enum 필드의 빈 상태 초기값
(`empty{Entity}` 상수)이 default 값을 PascalCase로 바꿔 넣고 있었다 — union 타입 자체가
wire 이름으로 바뀌면 그 리터럴은 더 이상 타입의 멤버가 아니게 되어 `tsc` 오류가 난다. 같은
default가 C# 쪽에서 PascalCase 멤버 표현식(`Status.Draft`)으로 옳게 렌더링되는 것과는 다른
자리 — TS 쪽은 멤버 표현식이 아니라 리터럴이므로 wire 이름 그대로 써야 한다.

### 파괴적 변경 — 재생성 시 TS 산출물 값이 바뀐다

`enums_gen.ts`의 union 리터럴, `enum_labels_gen.ts`의 객체 키·`Exclude<>` 타입 인자, 그리고
`@default(...)`가 붙은 enum 필드의 폼 빈 상태 초기값이 CLR 스타일(`'InProduction'`)에서
M3L 원문(`'in_production'`)으로 바뀐다. 라벨 텍스트 자체(사람이 읽는 문자열)는 바뀌지 않는다
— description이 없는 값의 표시 폴백만 그대로 PascalCase를 쓴다. 이 값을 문자열 비교하는
소비앱 코드(예: `status === 'InProduction'`)는 재생성 후 갱신이 필요하다 — 서버가
`[EnumMember]`를 따르는 한, 새 값이 실제 wire 계약과 일치한다.

## 0.13.1

### 🔴 enum CHECK 제약(opt-in)이 apply→diff 를 영원히 수렴하지 않게 만들었다 (버그)

`emitEnumCheckConstraints` 는 처음 나온 순간부터 이 문제 때문에 기본 off 였다. 근본 원인을
라이브 SQL Server 로 실측했다: **SQL Server 는 `CHECK (col IN (v1, v2, …))` 를 문자 그대로
저장하지 않는다** — 제약이 만들어지는 순간 **역순 OR 체인**으로 다시 쓴다
(`sys.check_constraints.definition` 이 `CHECK (col IN (a, b))` 를
`([col]=b OR [col]=a)` 로 돌려준다. 값이 하나면 그냥 `=` 로 접힌다). 선언형 비교 도구가
그 라이브 정의를 우리 `IN (…)` 원본과 비교하면 **영원히 다른 모양으로 보여**, 몇 번을
apply 해도 재-diff 가 수렴하지 않았다.

생성기가 처음부터 SQL Server 가 실제로 저장하는 모양(역순 OR 체인)으로 방출하도록 고쳤다.
라이브 SQL Server 2022 컨테이너로 처음부터 끝까지 검증: 기존 테이블에 제약을 새로 추가 →
apply → 재-diff 가 **첫 시도에 변경 0건**(이전에는 같은 DROP+ADD 가 계속 반복됐다).

**Postgres 타깃은 이 수정의 영향을 받지 않는다** — 이미 자기만의 재작성 형태
(`= ANY(ARRAY[...])`)로 저장하며, 그쪽이 같은 문제를 겪는지는 별도 검증이 필요해 이번에는
다루지 않았다.

### 파괴적 변경 없음

`emitEnumCheckConstraints` 를 켠 소비자는 재생성 시 제약 정의 텍스트가 바뀐다(같은 의미의
다른 모양) — 다음 apply 한 번으로 반영되고, 그 apply 이후로는 재-diff 가 안정된다. 옵션
자체는 여전히 기본 off — 이 릴리스는 이유였던 결함만 고쳤을 뿐, 기본값 재검토는 별개다.

---

## 0.13.0

### ⚠️ `emitForeignKeyIndexes` 기본값이 `false` → `true`로 바뀐다 (Sql·Postgres 타깃, 동작 변화)

`@reference` FK마다 EF는 관례상(`ForeignKeyIndexConvention`) 인덱스를 만들지만, Sql/Postgres
타깃은 이 옵션이 꺼져 있는 한 만들지 않았다 — 같은 선언을 두 타깃이 서로 다른 스키마로
번역하고 있었다. 실측(소비자 1곳, 43모델·51`@reference` 기준): 실제로 방출되는 인덱스는
**11개(≈17%)** — 나머지는 PK/UNIQUE/복합 인덱스의 선행 컬럼으로 이미 덮여 있었다. 켜기·끄기
둘 다 `hasDestructiveChanges=false`(데이터 손실 없는 인덱스 생성/삭제)로 확인됐다.

**기본값을 켜서 두 타깃을 정렬한다.** 재생성하면 이전에 커버되지 않았던 FK 컬럼마다
인덱스가 새로 방출된다 — 정확히 몇 개인지는 모델마다 다르다(위 11개는 한 소비자의 실측값이지
일반 비율이 아니다).

**되돌리려면**: `mdd.json`의 해당 타깃에 `"emitForeignKeyIndexes": false`를 명시하면 0.12.x
동작(인덱스 미방출)으로 복귀한다.

**배포 전 확인할 것**: schemorph 같은 선언형 도구를 쓰는 소비자는 재생성 후 `diff`(또는
`diff --format sql`)로 실제로 몇 개가 어디에 생기는지 먼저 본다 — 인덱스 생성/삭제는
`--allow-destructive` 게이트에 걸리지 않으므로 예상 밖의 인덱스가 있어도 조용히 적용된다.
schemorph 최소 버전은 `>= 0.7.0`(그 미만의 postgres provider는 인덱스 차이를 `compare_failed`
로 거절해 테이블·컬럼 비교까지 함께 실패한다).

### 파괴적 변경 없음

기본값 전환 자체는 스키마에 인덱스를 추가할 뿐 데이터를 건드리지 않는다. 옵션을 껐다가
되돌리는 것도 비파괴적으로 확인됐다(위 참조).

---

## 0.12.4

### 🔴 0.12.3의 체이닝 lookup 수정이 자기참조·상호참조 모델에서 배포 불가능한 순환 뷰 의존성을 만들었다 (회귀)

0.12.3은 `@lookup(fk.field)`의 대상 컬럼이 파생(Lookup/Rollup/Computed) 필드일 때 JOIN을
대상의 `{Target}FullView`로 돌리도록 고쳤지만, 그 조건을 "요청한 컬럼이 실제로 파생 컬럼인가"
가 아니라 "대상 모델이 FullView를 하나라도 갖는가"로 넓게 적용했다. 그 결과:

- **자기참조 FK**(예: `parent_id: identifier? @reference(SameModel)?`)의 lookup이 **raw**
  베이스 컬럼(`name` 등)을 가리켜도 그 모델 자신의 FullView와 JOIN — 뷰가 자기 정의 안에서
  자기 자신을 참조해 배포 순서를 계산할 수 없었다.
- **Rollup과 역방향 Lookup이 같은 두 모델 사이에 있으면** 순환이 생겼다 — Rollup 서브쿼리는
  이미 대상에 파생 필드가 있으면 무조건 `{Target}FullView`를 소스로 삼고 있었는데(0.12.2
  이전부터), 여기에 반대 방향 Lookup까지 같은 넓은 조건으로 FullView를 향하면 A↔B 순환이
  완성됐다. `count` 집계처럼 대상 컬럼을 전혀 읽지 않는 Rollup도 마찬가지로 불필요하게
  FullView로 리다이렉트됐다.

`mdd build`는 두 경우 모두 에러 없이 성공하지만, 생성된 뷰 정의를 배포하는 선언형 스키마
도구는 순환 의존성을 거부한다.

고쳐서 조건을 정확하게 좁혔다 — Lookup JOIN과 Rollup 서브쿼리 둘 다, **요청한 그 컬럼이
대상 모델 자신의 Lookup/Rollup/Computed 필드일 때만** `{Target}FullView`를 향한다. raw
베이스 컬럼을 읽는 경우(자기참조 포함)와 `count` 집계는 항상 베이스 테이블을 그대로 쓴다 —
0.12.2 이전과 동일한 동작이다. 체이닝된 파생 컬럼 lookup(0.12.3이 원래 고치려던 경우)은
그대로 FullView를 향한다.

### 파괴적 변경 없음

수정은 순환을 만들던 자리에서만 생성 결과를 바꾼다(리다이렉트를 되돌려 베이스 테이블을
가리키게 함). 체이닝된 파생 컬럼 lookup의 생성 결과는 0.12.3과 동일하다. 재생성 시 자동으로
반영된다.

---

## 0.12.3

### 🔴 슬롯 플레이스홀더 라벨이 중괄호를 포함하면 생성 폼이 파싱 실패했다 (버그)

`@reference`/`@slot` 필드의 슬롯 플레이스홀더는 필드 라벨을 JSX 텍스트 위치에 그대로
삽입했다. 라벨에 `{`나 `}`가 있으면 그 문자가 JSX 표현식 컨테이너로 재해석되고, 그 안의
콤마는 JS 콤마 연산자로 파싱되어 빌드 도구가 명시적으로 거부했다 — `tsc --noEmit` 은 통과하고
번들러 트랜스폼 단계에서만 드러나는 종류의 실패였다.

같은 파일의 다른 모든 컨트롤은 라벨을 속성값(`label="..."`)으로 넣어 이미 안전했다 — 텍스트
노드로 나가는 슬롯 플레이스홀더 한 자리만 안전하지 않았다. 라벨을 JS 문자열 리터럴로 감싸는
것으로 고쳤다. 렌더링 결과는 이전과 동일한 텍스트다.

### 🔴 체이닝된 lookup 이 Lookup 필드를 다시 가리키면 생성된 뷰가 존재하지 않는 컬럼을 참조했다 (버그)

`@lookup(fk.field)` 에서 `field` 가 대상 모델 자신의 Lookup(또는 Rollup/Computed) 필드일 때,
생성된 `{Name}FullView` 의 JOIN 은 여전히 대상의 **베이스 테이블**을 향했다 — 그 컬럼은
베이스 테이블이 아니라 대상의 `{Target}FullView` 에만 존재한다. `mdd build` 는 에러 없이
성공했지만, 뷰 정의를 검증하는 선언형 스키마 도구는 존재하지 않는 컬럼 참조를 거부했다.

Rollup 서브쿼리가 이미 같은 상황(대상이 파생 필드를 가질 때 `{Target}FullView` 를 소스로
쓰는 것)을 처리하고 있었다 — Lookup JOIN 빌드만 그 처리를 따르지 않고 있었다. 같은 처리를
재사용해 고쳤다.

### 파괴적 변경 없음

두 수정 모두 재생성 시 자동으로 반영된다. 설정 변경도 소비자 조치도 없다.

---

## 0.12.2

**생성기 동작은 바뀌지 않는다** — 문서와 테스트뿐이다. 재생성한 산출물은 0.12.1 과 동일하다.

### 소비 프로젝트 계약이 **동작 계약**임을 밝힌다

README 「소비 프로젝트 계약」의 표를 **이름 목록으로 읽으면 틀린다.** `formControlsImport` 가
가리키는 모듈이 `UInput`·`USelect` 를 그 이름으로 export 하더라도, `onChange` 로 **값이 아니라
이벤트 객체를 넘기면** 계약은 충족되지 않는다. 웹 컴포넌트를 통과형으로 감싼 React 바인딩이
대표적으로 이 형태이며, TS strict 에서는 컴파일이 깨지고 느슨한 설정에서는 **이벤트 객체가 폼
모델에 저장된다.**

그 경우 필요한 것은 이름을 바꾸는 재수출이 아니라 **어댑터**(이벤트→값 변환, 배열 `options`→
자식 요소 투영, 프롭 이름 번역)다. 계약 절이 이제 그 사실과 판별 기준을 함께 적는다.

세 절의 소제목도 **경로가 아니라 옵션**을 앞세운다 — `#### formControlsImport 가 가리킬 모듈
(기본값 …)`. 종전 표기는 계약이 기본 경로에만 적용되는 것처럼 읽혔다.

그리고 **그 계약이 정확한 채로 유지되게** 했다. 방출되는 프롭이 계약 절에 없으면 테스트가
깨진다 — `step`·`maxlength` 가 0.8.0 에서 표에 반영된 것은 누가 기억했기 때문이었고, 이제
기억이 필요 없다. 계약을 테스트에 복제하지 않고 **README 를 읽는다**: 복제하면 두 곳이 어긋나고
그때도 알아차리는 것은 소비자다.

### 0.11.0 의 네임스페이스 안내가 **어느 버전부터인지** 말한다

0.11.0 은 생성 코드에서 완전수식명을 걷어내며 *"어떤 줄이 필요한지는 런타임 패키지의 README 가
안내한다"* 고 적었다. **그 안내가 가리킨 절은 런타임 패키지 0.10.0 에서 추가됐다** — 더 낮은
버전을 참조 중인 소비자는 지시를 정확히 따라가도 **동봉된 README 에서 그 절을 찾지 못한다.**
해당 항목이 이제 최소 버전과 절 이름을 함께 밝힌다.

## 0.12.1

> **0.11.1 은 이 문서 변경을 0.11.0 위에 얹은 패치다.** 게시 순서가 0.12.0 뒤가 됐으나
> 0.11 라인의 정상 패치이며, 0.12 를 쓰는 소비자는 이 릴리스를 받으면 된다.

### 릴리스 노트가 nuget.org 에서도 보인다

`PackageReleaseNotes` 가 비어 있었다. CHANGELOG 는 0.8.0부터 패키지에 동봉돼 왔지만,
nuget.org 가 「Release Notes」라는 이름으로 노출하는 자리는 빈 채였다 — **소비자는 그 이름을
찾고, 없으면 릴리스 노트가 없다고 읽는다.** 그 자리가 이제 CHANGELOG 를 지시한다.

머리말에 **건너뛴 구간을 전부 읽으라는 안내**도 넣었다. 각 릴리스는 자기 breaking 만 적으므로
최신 항목 하나만 읽으면 건너뛴 구간의 동작 변화가 그대로 남는다.

### 동봉 명세 사본이 생성기의 실제 동작을 말한다

`docs/M3L.md` §2.3.1.1 이 의미 타입 축약을 언어 명세 그대로만 적고 있었다. 그래서 두 가지가
어긋나 있었다 — `money` 의 확장형이 명세와 다른 값(`decimal(10,2)`)이었고, **이 생성기가
그 표를 그대로 구현하지 않는다는 사실이 어디에도 없었다.**

이제 그 절이 세 가지를 밝힌다: 상한과 형식 검증이 한 쌍이며 `phone` 만 정규화 후 형태의
상한이라는 것, 이 생성기의 `phone` 이 30(명세 20)이며 **축소는 저장 데이터를 보지 못하는
생성기가 대신 결정할 수 없다**는 것, 그리고 `money`·`percentage` 는 미구현이라 선언 시
빌드가 실패한다는 것. 형식 검증은 어느 타입에도 방출되지 않는다.

## 0.12.0

### 생성 폼이 소비자의 폴더 구조를 더 이상 강제하지 않는다

생성된 `{Entity}Form_gen.tsx` 는 컴포넌트·레이아웃·헬퍼 모듈을 **고정된 경로 문자열**로
임포트했다. 생성물은 `DO NOT EDIT` 이므로 그 경로는 소비자가 고칠 수 있는 기본값이 아니라
**소비자가 반드시 만들어야 하는 폴더 배치**였다 — 이미 쓸 만한 컴포넌트 라이브러리를 가진
프로젝트도 그 경로에 재수출 파일을 두어야 했고, 패키지 경계를 넘는 모노리포 배치에서는
상대경로가 성립하지 않았다.

세 개가 설정으로 열렸다(TypeScript 타깃):

| 키 | 기본값 | 공급하는 것 |
|---|---|---|
| `formLayoutImport` | `@iyulab/enterprise` | `FormSection` · `FormRow` |
| `formControlsImport` | `../components/ui` | `UInput` · `UTextarea` · `USelect` · `UCheckbox` |
| `formSelectOptionsImport` | `../lib/select-options` | `enumToOptions` |

**기본값은 종전 문자열 그대로다.** 설정을 추가하지 않은 프로젝트의 생성물은 변하지 않는다.
기본값은 추천이 아니라 **호환을 위해 유지되는 값**이며, 어느 라이브러리를 가리킬지는
소비자 결정이다. 계약은 여전히 *"이 export 들이 필요하다"* 까지이고 *"어디에 두라"* 가 아니다.

### 🔴 생성 폼이 생성 타입을 못 찾을 수 있었다 (버그)

`outputPath` 와 `formsOutputPath` 는 서로 독립적인 설정인데, 폼은 생성 타입을
`'../types/entities_gen'` 라는 고정 문자열로 임포트했다. 즉 그 임포트는 *"폼 디렉터리의
부모 아래에 `types` 라는 이름으로 타입 디렉터리가 있다"* 가 참일 때만 해소된다. 그 관계를
검증하는 곳은 없었다.

어긋난 조합에서는 **생성이 성공을 보고하고, 실패는 소비 프로젝트의 컴파일 단계에서**
드러났다. 이제 이 경로는 두 설정에서 **계산**된다 — 어떤 조합이든 폼은 생성된 타입을 찾는다.
상대경로가 성립하지 않는 조합(서로 다른 드라이브·루트)에서는 **생성이 실패**하며, 컴파일되지
않는 파일을 남기지 않는다.

생성 타입 경로는 **설정 항목이 아니다.** 도구가 이미 아는 값을 되묻는 옵션은 어긋날 수 있는
정본을 하나 더 만들 뿐이다.

### 재발 방지

새로 추가되는 임포트가 모듈을 문자열로 지목하면 테스트가 깨진다. 방출된 모든 모듈 지정자는
**설정에서 왔거나, 출력 경로에서 계산됐거나, 이유가 기록된 예외**여야 한다(현재 예외는
`react` 하나 — React 컴포넌트가 React 를 구성 가능한 위치에서 가져올 수는 없다).

### 파괴적 변경 없음

설정을 추가하지 않으면 산출물이 동일하다. 소비자 조치는 없다.

---

## 0.11.0

### 타입이 함의하는 길이 상한이 모든 타깃에 도달한다

`email`·`phone`·`url` 은 선언에 `(n)` 을 적지 않아도 상한을 가진다 — 언어 명세 §10.4.2가
확장형을 규정한다. 지금까지 그 상한은 **SQL 컬럼에만** 있었다. 판별자가 명시된 `string(n)` 만
읽었기 때문이고, 그래서 0.10.0이 `string(n)` 에 대해 닫은 결함이 이 타입들에는 그대로 남아
있었다 — 상한을 넘는 값이 API 검증을 통과해 데이터베이스에서 provider 원본 오류로 되돌아왔다.

이제 컬럼·C# 엔티티(`[StringLength]`)·필드 스키마(`maxLength`)·생성 폼(`maxlength`) **넷이
같은 값**을 쓴다. 네 산출물의 합의는 테스트로 고정된다.

### ⚠️ 상한 값이 바뀐다 — 명세와 어긋나 있었다

| 타입 | 명세 §10.4.2 | 0.10.0 | 0.11.0 |
|---|---|---|---|
| `email` | `string(320)` | 200 | **320** |
| `url` | `string(2048)` | 500 | **2048** |
| `phone` | `string(20)` | 30 | 30 (유지) |

**세 값 모두 명세와 달랐다.** 파서는 의미 타입을 확장하지 않고 이름 그대로 보고하므로,
확장 값은 각 소비자가 표로 들고 있어야 하고 그 표가 어긋나 있었다.

`email`·`url` 은 **넓어진다.** 생성 스키마가 바뀌므로 적용 시 컬럼 확장이 따라오며,
데이터 손실은 없다 — 오히려 지금까지 컬럼에서 거부되던 201~320자 값이 통과하게 된다.

`phone` 은 **30을 유지한다.** 명세를 따르면 축소이고, 이미 저장된 값이 새 상한을 넘으면
마이그레이션이 실패하거나 값이 잘린다. 생성기는 저장된 데이터를 볼 수 없으므로 그 결정을
대신 내리지 않는다. 이탈은 코드에 사유와 함께 기록돼 있고, 명세보다 **넓은** 방향이므로
명세가 허용하는 값이 거부되는 일은 없다.

### 업그레이드 시 확인할 것

- **`FieldSchema.maxLength` 에 새 항목이 나타난다.** 스키마를 읽어 자체 검증을 도는 클라이언트
  코드가 영향받는 유일한 표면이다.
- **폼의 `maxlength`** 는 저장된 값을 무효화하지 못한다 — 상한이 넓어지는 방향이라 더욱 그렇다.
- **형식 검증은 포함되지 않는다.** 321자 이메일은 거부되지만 `not-an-email` 은 그대로 통과한다.
  형식은 여전히 애플리케이션 계층의 몫이다.

### ⚠️ 생성 코드가 네임스페이스를 적지 않는다 — 소비 프로젝트 조치 필요

생성된 C# 이 지금까지 런타임 타입들을 완전수식명(`global::…`)으로 적었다. 이제 **단순명**만
적고, 이름을 어느 네임스페이스에서 찾을지는 소비 프로젝트가 정한다.

이 생성기가 남의 패키지 배치를 알고 있을 이유가 없다. 알고 있으면 그쪽에서 네임스페이스 하나만
옮겨도 이쪽 생성물이 깨지고, 고칠 곳은 생성기가 된다. 이름만 적으면 그 결합이 사라진다.

**조치** — 소비 프로젝트에 `global using` 을 선언한다. 어떤 줄이 필요한지는 **런타임 패키지
0.10.0 이상**의 README 「Namespaces a consumer needs」절이 안내한다 — 그 절은 0.10.0 에서
추가됐으므로, **더 낮은 버전을 참조 중이라면 동봉된 README 에는 그 안내가 없다.** 먼저 올릴 것.
누락 시 나오는 오류는 타입명을 지목하는 평범한 `CS0246` 이다.

> 필요한 네임스페이스를 여기 옮겨 적지 않는 것은 의도적이다. 이 생성기가 남의 패키지 배치를
> 아는 것이 위 문단이 제거한 결합이며, **그 지식을 코드 대신 문서에 적어도 같은 결합이다.**
> 정본은 그 패키지의 README 한 곳이고, 그래야 네임스페이스가 바뀔 때 고칠 자리도 한 곳이다.

`@implements` / `@inherits` 로 **모델이 직접 적은 완전수식명**은 그대로 유지된다 —
그 이름은 모델 작성자가 고른 것이지 생성기가 고른 것이 아니다.

### 같은 결함이 다시 숨지 못하게 하는 것

이 결함이 오래 보이지 않은 이유는 값이 틀려서가 아니라 **검사가 그 축을 보지 못해서**다.
세 가지를 함께 고쳤다.

- **길이 상한의 정본이 하나가 됐다.** 두 방언 매퍼가 각자 들고 있던 사본을 없앴다.
- **컬럼 대조 검사가 문자열 아닌 상한도 본다.** 어느 분류에도 들어가지 않는 상한 있는 컬럼은
  이제 판단을 적기 전까지 검사를 통과하지 못한다. `binary(n)` 의 상한이 엔티티에 도달하지
  않는다는 사실은 **기록으로 단언**된다 — 검사에서 제외하면 그 축에 대해 다시 침묵하게 된다.
- **타입 어휘에 처분 표가 생겼다.** 선언 어휘에만 있던 래칫의 짝이다. 타입에 암묵 상한을
  주면서 엔티티로 나르지 않는 것이 이제 불가능하다.
- **생성물에 생성기가 스스로 붙인 완전수식명이 남지 않는다**는 것을 전체 파이프라인 산출물
  대상으로 검사한다. 모델이 적은 완전수식명과 BCL 참조만 예외다.

## 0.10.0

### 선언된 제약이 C# 엔티티까지 도달한다

모델이 선언한 널 허용 여부·길이 상한·기본값은 지금까지 SQL 컬럼과 생성 폼에는 반영됐지만
**C# 엔티티에는 반영되지 않았다.** 그래서 API 표면이 모델이 금지한 값을 받아들였고, 위반은
데이터베이스까지 내려가 provider 원본 오류로 되돌아왔다. 세 축을 모두 방출한다:

| 선언 | 방출 |
|---|---|
| 널 허용하지 않는 참조형 필드(`string`/`text`/`json`/`phone`/`email`/`url`/`binary`) | `[Required]` |
| `string(n)` | `[StringLength(n)]` — 널 허용 여부와 무관 |
| `= <value>` | C# 속성 초기화자 (`= true;` · `= 3;` · `= "NEW";` · `= 0.5m;` · `= Status.Draft;`) |

`@not_null` 을 적었는지가 아니라 **필드가 실제로 널을 허용하는지**가 기준이다 —
`- name: string(50)` 처럼 속성 없이 선언한 필드도 컬럼이 `NOT NULL` 이므로 함께 방출된다.

파생 필드(`@lookup`/`@rollup`/`@computed`)에는 `[Required]` 가 붙지 않는다. 뷰가 채우는 값이라
호출자가 보낼 수 없고, 붙이면 생성 요청이 전부 거부된다.

기본값은 **C# 리터럴로 표현할 수 있는 타입에만** 방출된다. 시간 타입·`identifier`·`binary` 는
기본값이 무엇이든 초기화자를 만들지 않으므로, `= now()` 같은 서버 측 기본값이 코드로 새지 않는다.

### ⚠️ 소비자 조치 필요 — 지금까지 통과하던 요청 일부가 400 이 된다

두 가지 이유로 동작이 바뀐다. 둘 다 **모델이 이미 선언하고 있던 것**을 집행하는 것이지만,
그동안 조용히 통과하던 호출자가 있었다면 업그레이드 시 드러난다.

**1. 빈 문자열이 거부된다 — `NOT NULL` 보다 좁다.**
SQL `NOT NULL` 컬럼은 빈 문자열 `''` 를 **허용**하지만 `[Required]` 는 거부하며, 판정 전에
문자열을 trim 하므로 **공백만 있는 값도 거부**한다. 즉 널 허용하지 않는 문자열 필드는 이제
API 표면에서 "값이 있어야 한다"를 뜻한다. 빈 문자열을 정상 값으로 저장하고 있었다면 그 호출은
실패하게 된다.

**2. 길이 초과가 데이터베이스에 닿기 전에 거부된다.** 이전에는 컬럼에서 잘리거나 실패했다.

부분 수정(PATCH)은 영향받지 않는다 — 델타 바인딩은 보내지 않은 속성을 검증 대상으로 삼지 않으므로,
필수 필드를 포함하지 않는 부분 수정은 그대로 성공한다.

선언된 기본값을 가진 필드는 **초기화자가 함께 방출되므로** 그 필드를 생략한 요청이 새로
거부되는 일은 없다. 다만 그동안 CLR 기본값(`false`/`0`)이 저장되던 필드에는 이제 **선언한 값이**
저장된다 — `boolean = true` 로 선언한 필드가 생성 시 `false` 로 저장되던 동작이 고쳐진 것이다.

### 내부 — 타깃 간 제약 처분 래칫

선언 어휘 전체에 대해 **Model 타깃이 그것을 어떻게 처리하는지**를 기록하고, 새 어휘가 추가되면
처분이 기록될 때까지 테스트가 실패한다. 한 타깃에만 반영하고 다른 타깃에서 잊는 일이 아무 신호
없이 지나가던 것을 막는다. 현재 기록된 타깃 간 비대칭: `@unique` · `@index` · `@min` · `@max` · `@help`.

### 내부 — acceptance 게이트가 어디서나 돈다

기능 행렬을 한 번에 교차하는 대규모 모델(24 엔티티 + 15 enum)이 리포에 들어왔다. 게이트는 그
모델로 4-타깃 빌드를 돌린 뒤 산출물을 **선언과 이름 단위로 대조**한다 — 기대값을 파싱한 모델에서
도출하므로 모델을 키워도 테스트의 숫자를 고칠 일이 없고, 산출물이 빠진 선언은 개수 차이가 아니라
이름으로 보고된다.

이전 게이트는 리포 밖 경로를 참조했고 그 경로가 없으면 **조용히 통과**했다 — 경로를 가진 한
대에서만 실제로 실행되고 나머지 모든 실행에서는 아무것도 검증하지 않으면서 통과 수에는 포함됐다.
`MDDBOOSTER_ACCEPTANCE_MODEL` 로 다른 모델을 지정할 수 있지만, 지정한 경로가 없으면 이제
**건너뛰지 않고 실패**한다.

### 외래 키 인덱스 opt-in — `emitForeignKeyIndexes` (Sql 타깃, 기본 `false`)

두 엔진 모두 외래 키를 자동 인덱싱하지 않는다. 그래서 FK 로 하는 조인과, 삭제 시 참조 행을
확인하는 검사가 자식 테이블을 훑는다. 켜면 **모델이 덮지 않은 FK 컬럼마다** 인덱스를 만든다.

```json
{ "type": "Sql", "projectPath": "../db", "emitForeignKeyIndexes": true }
```

- **방언 중립** — T-SQL(`IX_{Model}_{Column}`)과 PostgreSQL(`ix_{table}_{column}`) 양쪽.
  어느 컬럼이 대상인지는 한 판정기가 정하므로 두 방언이 어긋날 수 없다.
  (한 가지 예외를 알고 있다 — 널 허용 유니크 컬럼은 T-SQL 에서 filtered index 가 되어 일반
  조인에는 쓰이지 않지만 판정기는 양쪽 모두 덮인 것으로 본다. 좁은 형태이고, 고치려면 판정기가
  방언을 알아야 해서 유지했다. 코드 주석에 기재.)
- **이미 덮인 컬럼은 건너뛴다** — `@pk`·`@unique`·`@index` 는 물론, **복합 인덱스·복합 유니크의
  선두 컬럼**도 대상이 아니다. `(a, b)` 인덱스는 `a` 만으로 하는 조회도 처리하므로 `a` 에
  인덱스를 또 만들면 쓰기 비용만 늘어난다.
- **기본값은 `false`** — 켜는 것은 읽기 이득과 쓰기·저장 비용의 교환이고, 기존 스키마에 조용히
  적용할 판단이 아니다. 끈 상태의 산출물은 이전과 같다.

> 24 엔티티 모델 실측: FK 제약 28개 · 모델이 선언한 인덱스 13개 · 옵션을 켜서 늘어난 인덱스
> **9개**(22개). 선두 컬럼 인지가 없었다면 이미 덮인 컬럼에 인덱스가 더 붙었을 것이다.

### PostgreSQL — `@index` 선언이 인덱스로 방출된다

PG 렌더러는 `@index`(필드 선언과 `### Indexes` 섹션 양쪽)를 **방출하지 않고 경고**했다. 근거는
선언형 적용 도구가 인덱스를 지원하지 않는다는 것이었고, 그 조건이 해소됐다.

```sql
CREATE TABLE public.facility_inspection ( … );

CREATE INDEX ix_facility_inspection_status ON public.facility_inspection (status);
```

- 인덱스는 `CREATE TABLE` **뒤의 독립 문**으로 나간다.
- PK·UNIQUE 제약이 소유하는 인덱스는 중복 방출하지 않는다 — 선언 상태 비교에서 제약이 소유한
  인덱스는 제외되므로, 중복을 내면 매 적용마다 살아남는다.
- `CONCURRENTLY` 는 쓰지 않는다 — 적용이 단일 트랜잭션이고 concurrent 빌드는 그 안에서 실행될
  수 없다.
- 인덱스명(`ix_{table}_{cols}`)도 제약명과 같은 63바이트 게이트를 통과해야 한다. 다중 컬럼
  인덱스명은 제약명보다 쉽게 넘기며, 넘기면 조용히 절단되는 대신 빌드가 실패한다.

> **선언이 없으면 산출물은 바뀌지 않는다.** 이 변경은 `@index` 를 적은 모델에만 효과가 있다.
> FK 컬럼에 인덱스를 자동으로 만드는 정책은 **별개**이며 아직 없다.

### 내부 — snake_case → PascalCase 변환을 한 곳으로

같은 변환의 손구현 사본이 네 생성기에 걸쳐 **12벌** 있었다(동작은 전부 같았고, 하나만 빈 문자열
가드가 없었다). `NameCasing.ToPascalCase` 로 모으고 사본을 제거했다. 산출물은 네 타깃 전부
바이트 단위로 동일하다.

정리 문제가 아니라 정확성 문제가 됐기 때문이다 — enum 멤버명은 **enum 선언에서 한 번, 그 멤버를
참조하는 속성 초기화자에서 다시 한 번** 올려지고 둘은 서로 다른 렌더러가 낸다. 두 변환이
어긋나면 존재하지 않는 멤버를 가리키는, **컴파일되지 않는 초기화자**가 나온다.

### 문서 정정

`@unique(col1, col2)` 복합과 `### Indexes` 섹션이 기능 표에 미구현(⏳)으로 남아 있었으나
**둘 다 구현돼 있다.** 빌드 실행 예시도 동작하지 않는 형태였다(`mdd <dir>` → `build` 가 필수).

### 설치 스크립트 기본 경로

`scripts/install.ps1` 의 기본 설치 경로가 `%LOCALAPPDATA%\Programs\mdd-booster` 가 됐다
(이전에는 특정 드라이브 문자를 가정했다). `-InstallPath` 로 계속 지정할 수 있다.

---

## 0.9.0

### ⚠️ 소비자 조치 필요 — 이름이 바뀐다 (대문자 약어로 시작하는 엔티티가 있는 경우)

엔티티명이 **대문자 약어로 시작하면**(`QRScanLog`, `QRCode`) 지금까지 camelCase 변환이
첫 글자만 소문자화해 어색한 이름을 만들었다. `.NET JsonNamingPolicy.CamelCase`와 같은
표준 규칙으로 정정했다:

| 표면 | before | after |
|---|---|---|
| GraphQL query / mutation | `("qRScanLogs", "qRScanLog")` | **`("qrScanLogs", "qrScanLog")`** |
| 생성 폼 헬퍼 (`*Form_gen.tsx`) | `export function qRScanLogFromEntity` | **`export function qrScanLogFromEntity`** |
| OData entity set | `"QRScanLogs"` | `"QRScanLogs"` — **변경 없음** (DbSet 이름과 맞춤) |

후행 약어는 보존한다: `OrderQR` → `orderQR`.

**재생성만으로 회수되지 않는 곳이 하나 있다 — 손으로 쓴 `AddEntityPair` 호출부.**
생성물 밖에서 `options.GraphQL.AddEntityPair<…>("qRScanLogs", "qRScanLog")` 를 직접 적어 둔
컴포지션 코드가 있으면 **컴파일 오류가 나지 않는다** — 그 서버만 옛 필드명을 계속 노출하고,
재생성된 다른 서버는 새 필드명을 노출해 **같은 엔티티가 서버마다 다른 이름으로 보인다**.
업그레이드 시 `AddEntityPair(` 를 검색해 손으로 적은 인자를 함께 고칠 것.

기존 GraphQL 쿼리가 옛 필드명을 참조하고 있으면 그 쿼리도 함께 갱신해야 한다.
(영향 판정 방법: 모델 정본에서 `^[A-Z]{2,}` 로 시작하는 엔티티가 없으면 이 릴리스는 무영향이다.)

### ⚠️ 소비자 조치 필요 — `EntitySetName` union 이 좁아진다 (`@internal` 모델이 있는 경우)

`entity_names_gen.ts` 의 `ENTITY_NAMES`/`EntitySetName` 에서 **`@internal` 모델이 빠진다.**

이 목록은 OData entity set 이름의 미러이고 소비자가 이걸로 OData URL 을 만든다. 그런데
`@internal` 모델은 `AddEntityPair` 가 등록하지 않으므로 **그 이름들은 존재하지 않는 경로였다** —
타입세이프하게 404 를 광고하고 있었던 셈이다. 결함 수정이다.

`EntitySetName` 을 좁히므로, 그 이름을 쓰던 코드가 있으면 컴파일 오류가 난다(그 경로는
애초에 동작하지 않았다). **`entities_gen.ts` 의 인터페이스·`*Form_gen.tsx` 는 그대로 유지된다** —
타입은 데이터 API 전용이 아니고 전용 엔드포인트로 관리되는 엔티티에도 유용하기 때문이다.

### 추가 — 타깃별 엔티티 부분집합 (`includeEntities` / `excludeEntities`)

한 모델 정본을 여러 서버가 공유할 때, **표면 타깃(Api·TypeScript)** 이 노출할 엔티티를
타깃별로 좁힐 수 있다. 둘 다 생략하면 전량 — **완전 하위호환**이다.

```json
{ "type": "Api", "projectPath": "../src/MyApp.MesServer", "namespace": "MyApp.MesServer",
  "includeEntities": ["ProductionWork", "QRScanLog"] }
```

- 둘을 함께 지정 · 없는 엔티티명(가장 가까운 이름 제안 포함) · `includeEntities` 에 `@internal`
  모델 · `Sql`/`Model` 타깃에 지정 — **전부 빌드 오류**다. 조용히 무시하거나 부분 적용하지 않는다.
- 필터가 걸린 타깃은 빌드마다 커버리지를 출력한다(`포함 N개 / 제외 M개 — 이름…`).
- ⚠️ `includeEntities` 는 drift 한다 — 정본의 새 엔티티가 이 타깃에는 조용히 안 나타난다.
  신규 엔티티 기본 노출을 원하면 `excludeEntities` 를 쓸 것.

- **Api 타깃만 좁히면 경고**한다. `EntitySetName` 은 OData entity set 이름의 미러이므로 Api 표면만
  좁히면 어떤 서버도 등록하지 않는 이름을 계속 광고한다. 같은 필터를 TypeScript 타깃에도 지정하면
  사라진다. 판정은 전 Api 타깃의 합집합 기준이라 공유 UI 가 여러 서버를 담당하는 구성은 대상이 아니다.

전체 규칙표는 README "타깃별 엔티티 부분집합" 절 참조.

### 수정 — 복수 타깃의 조용한 오출력 2건이 빌드 오류로 올라갔다

한 정본을 여러 서버가 소비하는 구성에서 **빌드는 성공하는데 산출물이 틀린** 두 경우가 있었다.
둘 다 소비자가 컴파일 오류나 죽은 라우트를 만난 뒤에야 원인을 찾게 되는 형태였다.

- **`Model` 타깃이 2개 이상일 때** `Api` 타깃이 **첫 번째** Model 의 namespace 를 집고 나머지를
  조용히 무시했다 → 잘못된 `using` 방출. 이제 후보가 둘 이상이면 추론하지 않고 빌드 오류로
  **`entitiesNamespace` 명시를 요구**한다. 신규 옵션 `entitiesNamespace` (Api 타깃, 생략 시 종전대로 추론).
  Model 타깃이 하나인 기존 구성은 **무영향**이다.
- **같은 종류·같은 경로 타깃이 2개**면 나중 것이 앞선 것의 산출물을 조용히 덮어썼다 → 빌드 오류.
  경로는 정규화해 비교하므로 `../api` 와 `../x/../api` 도 중복으로 잡힌다.
  같은 종류라도 **경로가 다르면 정상**이다(복수 서버 시나리오).

### 내부 — 단어분리 규칙이 한 곳으로 모였다

PascalCase 단어분리(ADR-0001 §2.1)가 snake_case 경로에만 있고 camelCase 경로 두 곳은
각자 손구현 사본을 갖고 있어서 `QRScanLog`가 `qr_scan_log`(정상)와 `qRScanLog`(훼손)로
갈라졌다. `MddBooster.Core.Naming.NameCasing`이 분리기의 정본이 되고 두 사본을 제거했다.
GraphQL 복수형은 이제 **camelCase를 먼저 적용한 뒤 복수화**한다 — 순서가 반대면 두 글자
약어가 `QR` → `QRs` → `qRs`로 깨진다.

---

## 0.8.0

### ⚠️ 소비자 조치 필요 (TypeScript 타깃을 쓰는 경우)

생성 폼(`*Form_gen.tsx`)이 **새 프롭 2종을 방출**한다. 소비 프로젝트의
`../components/ui` 배럴에 있는 `UInput` 래퍼가 이들을 받아 아래 input으로 전달해야 하며,
**없으면 재생성 후 `tsc`가 TS2322로 실패한다.**

```ts
export function UInput(props: {
  label: string
  value: string
  onChange: (v: string) => void
  required?: boolean
  type?: string
  description?: string
  step?: number        // ← 0.8.0 신규. decimal 필드가 있는 모델
  maxlength?: number   // ← 0.8.0 신규. string(n) 필드가 있는 모델
}) { /* 아래 input으로 그대로 전달 */ }
```

- **`maxlength`가 더 넓게 영향한다.** `decimal`은 안 쓸 수 있어도 `string(n)`은 거의 모든
  모델에 있다. `decimal`을 쓰지 않는 소비자도 이 프롭은 거의 확실히 필요하다.
- 표기 주의: **소문자 `maxlength`** (React DOM의 `maxLength` 아님), 두 값 모두 **`number`**
  (생성물은 `step={0.0001}` · `maxlength={50}` 형태의 중괄호 숫자 리터럴을 방출한다).
- `description?`은 0.8.0 신규가 아니라 **기존 계약**이다. 래퍼에 없다면 어떤 필드든
  `@help("...")`를 붙이는 순간 같은 방식으로 깨진다 — 이번 기회에 함께 갖출 것.

전체 계약은 README의 "소비 프로젝트 계약 (TypeScript 타깃)" 절 참조.

### 추가 — 모델 타입 정보가 입력 어포던스로 전달된다

생성 폼이 모델의 타입 정보를 버리고 있던 지점을 메웠다. SQL 타깃(`DECIMAL(p,s)`,
`NVARCHAR(n)`)과 Model 타깃(`[Column(TypeName)]`)은 같은 정보를 이미 소비하고 있었고,
TypeScript 폼 타깃만 버리고 있었다.

- **`decimal(p,s)` → `step={10^-s}`** — `decimal(18,4)`는 `step={0.0001}`.
  이전에는 `step`이 없어 브라우저 기본값 1이 적용됐고, **소수 입력이 필요한 필드는 저장이
  불가능**했다("Value must be a multiple of 1"). 오류 없이 submit이 조용히 막히는 형태라
  앱에는 아무 신호도 가지 않았다.
  - 파라미터 없는 `decimal`은 `step={0.01}` (SQL 기본값 `DECIMAL(18,2)`와 일치).
  - 정수 타입과 `decimal(p,0)`은 `step`을 방출하지 않는다 — HTML 기본값 1이 정확히 맞다.
- **`string(n)` → `maxlength={n}`** — `NVARCHAR(n)`의 상한을 UI로 앞당긴다.
  저장된 값은 이미 n 이내이므로 기존 값을 무효화하지 않는다. 상한이 없는 무파라미터 `string`과
  `text`(둘 다 `NVARCHAR(MAX)`)는 방출하지 않는다.

### 알려진 한계 (의도적 미구현 — 문서화)

- **`float`/`double`은 여전히 소수 입력이 막힌다.** 스케일 개념이 없어 `step="any"`가 유일한
  정답인데, 소비자 계약상 `step`이 `number`라 `"any"`를 실을 수 없다.
  **소수가 필요한 필드는 `float`/`double` 대신 `decimal(p,s)`로 모델링할 것** —
  정밀도가 명시되므로 SQL·EF·폼이 모두 같은 약속을 하게 된다.
- **`timestamp`/`datetime`/`time`은 자유 텍스트 입력으로 남는다 — 이것은 결정이다.**
  `DateTimeOffset`이 직렬화하는 `"2026-07-28T14:30:00+09:00"`을 `<input type="datetime-local">`이
  받지 못하고(오프셋 불가), `TimeOnly`의 소수 초를 `<input type="time">`이 받지 못한다
  (기본 `step=60`, 소수 3자리 한계). 피커를 붙이면 브라우저가 값을 거부해 컨트롤이 **빈 칸**이
  되고 그대로 저장하면 **기존 값이 지워진다** — 값을 보여주기라도 하는 자유 텍스트보다 나쁘다.
  `date`만 피커를 받는 이유도 이것이다(`DateOnly` → `"2026-07-28"`은 컨트롤이 그대로 받는다).

### 내부

- `FieldAttributes.TypeParams` / `FieldAttributes.StringMaxLength`를 `MddBooster.Core`의
  정본으로 세우고, `EntityPairRenderer`·`TsFieldSchemaRenderer`가 각자 갖고 있던 동등 로직을
  제거했다. 같은 컬럼의 정밀도·길이를 두 곳이 각자 계산하던 상태를 해소.
